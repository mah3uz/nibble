<!DOCTYPE html>
<html lang="en" class="h-full<%= " dark" if cp_theme == "dark" %>">
  <head>
    <title>Nibble</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_stylesheet_tag "cp" %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag "inertia" %>
    <%= inertia_ssr_head %>
  </head>
  <body class="h-full">
    <%= yield %>
  </body>
</html>
