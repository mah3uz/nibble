#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Downloads the gem index to check, which is a cost a release should pay and a test run should not.
ARGV.unshift("--ensure-latest") if ENV["CI"]

load Gem.bin_path("brakeman", "brakeman")
